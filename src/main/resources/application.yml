spring:
  application:
    name: momenttum

  output:
    ansi:
      enabled: ALWAYS

  config:
    import:
      - optional:file:.env[.properties]

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:mydatabase}?createDatabaseIfNotExist=true
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none                # Disable Hibernate automatic schema creation
      format_sql: true
    defer-datasource-initialization: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

  sql:
    init:
      mode: always                    # Always run init scripts (for non-embedded DBs too)
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

  springdoc:
    api-docs:
      path: /v3/api-docs
    swagger-ui:
      path: /swagger-ui.html

  validation:
    fail-fast: false

logging:
  level:
    root: INFO
#    org.springframework.jdbc.datasource: DEBUG

server:
  port: ${SERVER_PORT:8080}


jwt:
  secret: ${JWT_SECRET_KEY:IPMaQEBhefMwnNhzvK9taBqokCl0KGNu+qTRiKsvmGU=}
  expiration-ms: 1800000
  refresh-expiration-ms: 604800000